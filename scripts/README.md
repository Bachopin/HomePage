# 开发脚本说明

本目录包含用于开发环境管理的脚本工具。

## 脚本列表

### 1. `dev-restart.sh` - 基础重启脚本
**功能**: 关闭3001端口服务，清理缓存，重启开发服务器

```bash
# 直接运行
./scripts/dev-restart.sh

# 或使用npm脚本
npm run dev:restart
```

**执行步骤**:
- 🔪 关闭3001端口进程
- 🔍 关闭其他Node.js开发进程  
- 🧹 清理.next目录和缓存
- 🗑️ 清理npm缓存
- ⏳ 等待端口释放
- 🚀 启动开发服务器

### 2. `dev-start.sh` - 智能启动脚本
**功能**: 端口检测、自动清理、健康检查、自动重试

```bash
# 直接运行
./scripts/dev-start.sh

# 或使用npm脚本
npm run dev:start
```

**特性**:
- ✅ 自动端口冲突检测和处理
- 🔄 启动失败自动重试 (最多3次)
- 🏥 服务健康检查 (30秒超时)
- 🎨 彩色日志输出
- 📊 详细的启动状态反馈

### 3. `dev-restart.bat` - Windows版本
**功能**: Windows系统的重启脚本

```cmd
# Windows命令行运行
scripts\dev-restart.bat
```

## NPM 脚本命令

在项目根目录可以使用以下npm命令：

```bash
# 标准开发启动
npm run dev

# 智能启动 (推荐)
npm run dev:start

# 重启开发服务器
npm run dev:restart

# 仅清理缓存
npm run dev:clean

# 强制关闭3001端口进程
npm run dev:kill
```

## 使用建议

### 日常开发
```bash
npm run dev:start    # 首次启动或遇到问题时
npm run dev         # 正常启动
```

### 遇到问题时
```bash
npm run dev:restart  # 快速重启
npm run dev:clean    # 仅清理缓存
npm run dev:kill     # 强制关闭端口
```

### 故障排除

**端口被占用**:
```bash
npm run dev:kill     # 强制关闭
npm run dev:start    # 重新启动
```

**缓存问题**:
```bash
npm run dev:clean    # 清理缓存
npm run dev         # 重新启动
```

**启动失败**:
```bash
npm run dev:start    # 使用智能启动脚本
```

## 脚本配置

### dev-start.sh 配置项
```bash
PORT=3001                    # 服务端口
MAX_RETRIES=3               # 最大重试次数
HEALTH_CHECK_TIMEOUT=30     # 健康检查超时(秒)
```

### 依赖要求
- **必需**: `npm`, `lsof` (macOS/Linux自带)
- **可选**: `curl` (用于健康检查)

## 注意事项

1. **权限**: 确保脚本有执行权限
   ```bash
   chmod +x scripts/*.sh
   ```

2. **系统兼容性**:
   - `*.sh` 脚本适用于 macOS/Linux
   - `*.bat` 脚本适用于 Windows

3. **端口冲突**: 脚本会自动处理3001端口冲突

4. **进程清理**: 脚本会安全地关闭相关进程，避免僵尸进程

## 自定义配置

如需修改端口或其他配置，请编辑：
- `package.json` 中的 `dev` 脚本
- `scripts/dev-start.sh` 中的配置变量